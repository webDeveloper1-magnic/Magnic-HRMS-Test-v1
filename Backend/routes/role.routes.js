const express = require("express")
const router = express.Router()

// Placeholder for role routes
router.get("/", (req, res) => {
  res.json({ message: "Role routes - Coming soon" })
})

module.exports = router
